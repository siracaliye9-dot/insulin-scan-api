services:
  - type: web
    name: insulin-scan-api
    env: node
    region: frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
